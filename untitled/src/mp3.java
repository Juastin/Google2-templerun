import static org.graalvm.compiler.lir.constopt.ConstantTreeAnalyzer.analyze;
final SerialPort comPort = SerialPort.getCommPort("COM3");
public class mp3 {
    public static void main(String[] args) {
        int notes = analyze(audioSignal);
    }
}
